# This file is intentionally left empty.
#
# The 'api-status-checker-lambda' function only uses libraries
# that are already included in the standard AWS Lambda Python 3.12 runtime
# (e.g., boto3, os, json, decimal).
#
# No external packages need to be installed via pip.